import { render, screen } from '@testing-library/react';
// import SplineFloorRecord from './SplineFloorRecord';

// TEST DISABLED: JEST DOES NOT SUPPORT SPLINETOOL

// Unit Test: SplineFloorRecord
// Element: Spline Canvas

// describe("Unit Test: SplineFloorRecord", () => {
//     test("Element Spline Canvas should exist", async () => {
//         const testData = {
//             limit: 100,
//             spending: 100
//         };
        
//         render(<SplineFloorRecord limit={testData.limit} spending={testData.spending}/>)
//         const splineElement = await screen.findByRole("canvas");
//         expect(splineElement).toHaveClass('spline-floor');
//     })
// })